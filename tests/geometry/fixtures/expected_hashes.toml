# SHA-256 baselines for the export module's regression tests.
#
# Key format (per research.md R6):
#   "<format>__<freecad_version>__<source_hash>__<kwargs_hash>"
#
# - format: one of fcstd | step | stl | brep
# - freecad_version: "<major>_<minor>_<patch>" from FreeCAD.Version() at write time
# - source_hash: SHA-256 (first 12 hex chars) of HullParameters.__repr__() for the
#   source hull, computed by tests/geometry/fixtures/refresh_hashes.py
# - kwargs_hash: SHA-256 (first 12 hex chars) of sorted kwargs JSON,
#   e.g. {"tessellation_tolerance": 0.001} for STL, {} for the others
#
# A FreeCAD-version bump is a TOML edit (run refresh_hashes.py on a host with
# the new FreeCAD installed, paste the new stanzas, bump PATCH, note in CHANGELOG).
# Per constitution VII, baseline drift across FreeCAD versions is allowed and
# documented — it is NOT a determinism violation.
#
# To seed this file:
#   uv run python tests/geometry/fixtures/refresh_hashes.py
# and paste the printed stanzas into this file.

# Seeded against FreeCAD 1.1.1
# Source: HullParameters() defaults (Storebro Royal Cruiser 34, 1972)
# Refreshed 2026-06-14: the source_hash (HullParameters().__repr__()) had drifted
# since the spec 007 seed; the porthole-placement fix (portholes now cut the hull
# instead of being doubled off it) and the spec 033 deeper-forefoot bow (stem
# keel_depth 0.08 -> 0.30 m) both change the geometry hashes. Regenerated via
# refresh_hashes.py on FreeCAD 1.1.1.

[step__1_1_1__0c04adf357b4__44136fa355b3]
sha256 = "d588c2a06234bd1f3b82115159d180c897a025590049501b3d39adec0637b09a"

[stl__1_1_1__0c04adf357b4__50ebc2c69987]
sha256 = "74a74f34ade04dc0e1ddd79f3a68805303874bba1f534b18528c7826f6e59ed3"

[brep__1_1_1__0c04adf357b4__44136fa355b3]
sha256 = "b244f72248dfba73120238162eb0f48592d2f08492cbab94ee399ce443ba331f"

[fcstd__1_1_1__0c04adf357b4__44136fa355b3]
sha256 = "44626dbc6ceee9d20a5250d3431efd865d9a4b8f24deac6d59d187652f8442c8"
